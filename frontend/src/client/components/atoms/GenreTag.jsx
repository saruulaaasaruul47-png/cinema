const GenreTag = ({ label }) => (
  <span className="px-3 py-1 text-xs font-semibold rounded-full border border-white/20 text-white/80 bg-white/5 tracking-wide uppercase">
    {label}
  </span>
);

export default GenreTag;
